# {{PROJECT_TITLE}}

## Setup

```
build.bat          # Windows
bash build.sh      # macOS / Linux
```

## Run

```
run.bat            # Windows
bash run.sh        # macOS / Linux
```

## Structure

- `cli/` — CLI front-end (argument parsing, console output)
- `lib/` — Core library (models, algorithms, processing logic)
- `data/in/` — Input data
- `data/out/` — Generated output data
